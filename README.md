# BLT Manager v3 - Mobile First

## Cách deploy
1. Giải nén zip.
2. Upload toàn bộ lên GitHub.
3. Streamlit Cloud chọn `app.py`.
4. Để giữ dữ liệu cũ, copy `hoc_vien.csv` và `lich_ban.csv` cũ vào thư mục gốc của project.

App vẫn đọc các file CSV cũ nên không cần nhập lại học viên.
